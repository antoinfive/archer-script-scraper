# ArcherScriptScraper

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add archer_script_scraper to your list of dependencies in `mix.exs`:

        def deps do
          [{:archer_script_scraper, "~> 0.0.1"}]
        end

  2. Ensure archer_script_scraper is started before your application:

        def application do
          [applications: [:archer_script_scraper]]
        end

